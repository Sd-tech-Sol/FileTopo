TASK_ID: TASK-0032 — V1 REAL_ROOT — Controlled Local Folder Onboarding (corrective pass)
AGENT: CLAUDE
RESULT: DONE
BRANCH: build/v0.2-a16-v1-real-root
FINAL_HEAD: cc0d46c
GO: a279ef9, child of efec2ea

SUMMARY:
- the independent control found two blocking defects. BOTH WERE REAL, and the previous delivery asserted the opposite on both points. Both are fixed on the same branch; TASK-0032 stays IMPLEMENTED, never self-VERIFIED.
- no new TASK or DEC. DEC-0033 corrected in D, H and I, with the wrong statements marked as corrected rather than quietly rewritten.

DEFECT_A — dialog:allow-open opened the boundary instead of closing it:
- established from the installed tauri-plugin-dialog 2.7.2 sources: permissions/autogenerated/commands/open.toml maps allow-open to commands.allow=["open"], and src/commands.rs shows `open` taking options with `default_path: Option<PathBuf>` FROM THE PAGE and returning the chosen paths TO the page — both forbidden by DEC-0033 A and B
- the permission was not needed for our picker; it was the hole. My own test demanded its presence, so it proved the hole instead of the guarantee.
- FIX: the capability carries `core:default` and nothing else. tauri_plugin_dialog::init() stays: a capability gates only WebView-reachable commands, never app.dialog() called from the host — verified in the plugin sources, whose FileDialogBuilder carries no permission check at all.
- PROOF at rest: Rust and TypeScript tests now require the ABSENCE of any dialog:, fs:, shell:, opener: and http: permission.
- PROOF at runtime (WebView2): a direct invoke of `plugin:dialog|open` — with and without defaultPath — and of `plugin:dialog|save` is refused by the permission layer before the plugin sees it. No dialogue opens, no real path is passed. Tauri names the missing permission itself: "dialog.open not allowed. Permissions associated with this command: dialog:allow-open, dialog:default". Recorded in the artefact as frontendDialoguePluginReachable: false.
- map_brain_choose_real_root still compiles and is still registered; the whole REAL_ROOT cycle still runs in the host.

DEFECT_B — a pre-DEC-0033 index could not be republished as the decision promised:
- publish_map ran `if reused { open_store(...) }`, and open_store requires a current binding. A TASK-0031 index carries none, so it was refused on EVERY path — refresh and rebuild included — and stranded forever. DEC-0033 D promised the opposite. The "deliberately broad refusal" the previous delivery declared as an assumed cost was in fact this defect.
- FIX: three doors instead of one. open_for_brain (membership), open_store (membership + current binding, required by any corpus read), check_publishable (membership + current binding OR the narrow legacy path).
- the legacy path opens only when ALL of these hold: the brain reads a SYNTHETIC_FIXTURE, the file is for this brain_id on a compatible canonical schema, it carries NEITHER source_kind NOR source_ref, and its fixture_id equals the catalogue's source_ref exactly. A REAL_ROOT never qualifies: no real root existed before DEC-0033, so an unbound index under one is not old, it is wrong.
- republication rescans and replaces in the same transaction: index_id kept, revision advanced only on successful publication, modern binding written then. A failure leaves the old index byte-identical and still refused by open.
- the check now runs BEFORE source resolution, so a disagreeing binding is refused without the catalogue even being consulted for a root.

SOURCE_BINDING_VERIFICATION:
- the binding checked is the PAIR source_kind + source_ref, on both open and publish. The same source_ref under a different source_kind is refused — the case the original single-key check let through.
- half a binding (one key without the other) was written by no version of this program and is refused everywhere.

PROOFS_ADDED:
- src-tauri/src/map/legacy_binding_tests.rs, B1-B5, built on an index reduced to the exact TASK-0031 shape. Its twelve metadata keys are written into the test (read off 05fc371:src-tauri/src/map/brain_index.rs) and asserted, so the test cannot drift into proving compatibility with a file that never shipped.
- B1: Legacy binding read back; map_open refuses with map_source_mismatch and map_view fails; file byte-identical after the refusal; explicit refresh succeeds with index_id kept, revision +1, binding acquired, indexReused true; map_open then succeeds with sourceRead false.
- B2: source removed under a Drop guard — refresh fails, file byte-identical, index still refused by open, still republishable later.
- B3: a REAL_ROOT whose binding was stripped is refused by open, refresh AND rebuild; nothing mutated and the source never read.
- B4: different source_ref refused; same source_ref under a different source_kind refused; legacy index naming another fixture not recognised; half a binding refused. No mutation in any case.
- B5: rebuild republishes a legacy index exactly as refresh does.
- src/map/realRoot.test.ts: capability expectation inverted; new guard that no interface source reaches for plugin:dialog, @tauri-apps/plugin-dialog, plugin:fs or @tauri-apps/plugin-fs.

VALIDATIONS:
- Rust 324 PASS, 0 failed, 5 ignored
- TypeScript 280 PASS, 18 files
- pnpm check, pnpm build, cargo build --offline, git diff --check: green
- cargo fmt --check: clean on every line this pass wrote, verified file by file; the pre-existing debt in hierarchy.rs, content_signals.rs and relation_commands.rs is untouched
- cargo clippy --all-targets --offline -- -D warnings: RED at 26 errors, the same count as before this pass. A 27th appeared — let_and_return in the new test file — and was fixed before delivery. R-T30-1 unchanged.
- WebView2 152.0.4191.66 replayed in full: the three permission refusals, then the unchanged REAL_ROOT cycle — 1210 nodes indexed, revisions 2 -> 3 -> 4 with indexId constant, 256 nodes and 4 aggregates under the 512 budget, absolutePathLeak false, source byte-identical, 0 fatal console errors, 4 trusted keydowns.

LIMITS:
- NEW, created by fix A and declared as such: the Rust-side dialogue call is not exercised at runtime — opening it would mean driving a Windows modal. That app.dialog() needs no permission was established from the installed plugin sources, not by an execution.
- DELIBERATE: the legacy compatibility path opens only for a synthetic brain whose fixture_id still matches. A legacy index whose fixture was renamed in the catalogue is not recognised and must be rebuilt from scratch; fixture_id is the only fact in the old metadata that ties the file to a source.
- unchanged from the first delivery: no personal brain and none requested; the native dialogue is not automated; no watcher, no incremental update, no FTS; no F-046 physical identity, no redesign; no performance/laptop acceptance; Registry/legacy_store debt NOT removed.

FILES_CHANGED:
- new: src-tauri/src/map/legacy_binding_tests.rs
- rust: map/brain_index.rs (IndexBinding + binding()), map/commands.rs (open_for_brain, open_store, check_publishable, check before source resolution), lib.rs (capability test inverted, plugin comment corrected), capabilities/default.json (dialog:allow-open removed)
- frontend: src/map/realRoot.test.ts (capability expectation inverted, plugin-command guard added)
- proof: scripts/task0032-webview2.mjs (runtime permission-boundary check, new limit), docs/performance/runs/TASK-0032-webview2.json (replaced)
- docs: DEC-0033 (D, H, I corrected), TASK-0032 (corrective section, stale audit note and stale claim corrected), CURRENT_STATE.md, NEXT_ACTION.md, HANDOFF.md, VALIDATION.md (section BC; the two superseded claims in BB struck through and pointed at BC), CHANGELOG_AI.md

X5: 36
MAIN_UNCHANGED: yes
TASK_STATUS: IMPLEMENTED
DECISION_STATUS: APPROVED
COMMIT: cc0d46c (corrective pass, code + proofs + docs)
PUSHED: yes — build/v0.2-a16-v1-real-root only, no PR, no merge, no tag, no release, no history rewrite
NEXT: independent control only
