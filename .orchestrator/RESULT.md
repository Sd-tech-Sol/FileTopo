TASK_ID: TASK-0032 — V1 REAL_ROOT — Controlled Local Folder Onboarding
AGENT: CLAUDE
RESULT: DONE
BRANCH: build/v0.2-a16-v1-real-root
FINAL_HEAD: 2ce3a17

SUMMARY:
- FileTopo can take a real local folder the person chose, without the path ever leaving the local catalogue and without reading a byte until somebody presses Indexer
- freeze c3507bf (TASK-0032 + DEC-0033 + the ACTION-0048 documentary correction) is the direct parent of the first code commit
- catalogue schema 2: REAL_ROOT beside SYNTHETIC_FIXTURE, a displayable source_label, an absolute path in a BLOB column the BrainRecord query does not select; migration rebuilds the table in one transaction and never touches catalog_meta, so active_brain_id survives by construction
- one product command, map_brain_choose_real_root, takes NO argument: the WebView cannot name a folder, only ask for the native dialogue; cancelling creates nothing; registering scans nothing
- BrainRecord carries no path field at all; the only door is BrainCatalog::real_root_path, returning a PathBuf, a type that is not Serialize
- path codec extracted from registry.rs into src/path_codec.rs, shared rather than copied, its non-Windows half corrected from to_string_lossy to raw OsStr bytes; the rest of the 0.1 Registry is untouched
- map/source.rs holds root validation (not a directory, a link or reparse point, and the three containment cases compared component by component) and source resolution; map_open calls neither
- the index stamps brain_id, source_kind and the opaque source_ref, never the path; open_store refuses a mismatched binding as map_source_mismatch and deletes nothing
- MapBuildReport.fixtureId becomes sourceKind + sourceRef + sourceLabel; fingerprints become nullable, null on a real root, with readOnlyConfirmed=false — "no fingerprint taken", never "read-only confirmed"
- reserve X2 lifted by DEC-0033 I and replaced: the dialogue plugin is initialised, the capability grants dialog:allow-open and nothing else, choose_collection stays unregistered, and a test reads the registered signatures to prove no exposed command lets the WebView name a place on disk
- MapApp gains one button and an explicit unindexed state read from what the composition failed to load; no redesign

REAL_ROOT_CONTRACT:
- picker explicit / no scan on registration: yes — map_open answers map_not_built and creates no file (RR2, WebView2)
- absolute path local-only / not serialized: yes — RR3 and the host run, which also inspect the index file and the host log
- first refresh explicit: yes — the button reads "Indexer" until the index exists, then "Actualiser"
- open index-only: yes — sourceRead=false, revision unchanged, DEC-0032 A intact
- source read-only: yes — full inventory byte-identical across refresh and rebuild (RR6, WebView2)
- bounded projection unchanged: yes — 1210 indexed nodes rendered as 256 nodes and 4 aggregates under the 512 budget

PRIVACY_PROOF:
- absolutePathLeak: false
- networkAdded: false
- sourceArtifacts: 0

VALIDATIONS:
- Rust 319 PASS, 0 failed, 5 ignored
- TypeScript 279 PASS, 18 files
- pnpm check, pnpm build, cargo build --offline, git diff --check: green
- cargo fmt --check: every line written by this task is clean, verified file by file; the pre-existing debt in hierarchy.rs, content_signals.rs and relation_commands.rs is untouched
- cargo clippy --all-targets --offline -- -D warnings: RED at 26 errors, the same count as on entry. The only diagnostic in a file touched here is collapsible_if on brains.rs::active(), code that predates this task (verified in git show HEAD:) whose line number merely moved. One diagnostic WAS introduced — too_many_arguments on BrainIndex::replace — and was fixed before delivery by grouping the three source facts into SourceStamp. R-T30-1 unchanged.
- real WebView2 152.0.4191.66, Tauri 2.11.5, SQLite 3.53.2, on a 1209-entry tree generated by the proof: 4 trusted keydowns, 0 fatal console errors

LIMITS:
- no personal brain, and none requested: every analysed tree is created by the proofs themselves and dies with them
- the native dialogue itself is not automated; its compilation, registration and primitive are proved, its opening is not
- the REAL_ROOT row for the host run is written by the seed, as TASK-0032 §7 allows
- no watcher, no incremental update
- no FTS
- no F-046 physical identity, no redesign
- performance/laptop acceptance still separate: 1210 nodes on a development workstation measures nothing of the sort
- Registry/legacy_store debt NOT removed; only the path codec was extracted
- deliberate broad refusal, declared: an index published before DEC-0033 carries no binding and is refused, never deleted; an explicit refresh republishes it

FILES_CHANGED:
- new: src-tauri/src/path_codec.rs, src-tauri/src/map/source.rs, src-tauri/src/map/real_root_tests.rs, src/map/realRoot.test.ts
- new: scripts/task0032-webview2.ps1, scripts/task0032-webview2.mjs, scripts/task0032-seed-proof.py
- new: docs/tasks/TASK-0032-v1-real-root.md, docs/decisions/DEC-0033-real-root-privacy-and-source-binding.md, docs/performance/runs/TASK-0032-webview2.json
- rust: lib.rs, registry.rs, map/mod.rs, map/brains.rs, map/brain_index.rs, map/commands.rs, map/sandbox.rs, map/content_signals.rs, map/lifecycle_tests.rs, map/projection_tests.rs, map/relation_commands.rs, capabilities/default.json
- frontend: src/map/MapApp.tsx, src/map/types.ts, src/map/composedScenario.ts and six test files updated for the new DTO shape
- docs: CURRENT_STATE.md, NEXT_ACTION.md, HANDOFF.md, VALIDATION.md (section BB), CHANGELOG_AI.md, FEATURE_MATRIX.md (F-001 row + dated note), TASK-0031 and CURRENT_STATE corrected to VERIFIED by ACTION-0048

X5: 36
MAIN_UNCHANGED: yes
TASK_STATUS: IMPLEMENTED
DECISION_STATUS: APPROVED
COMMIT: freeze c3507bf, code 7302291, host proof f00fb55, docs 2ce3a17
PUSHED: yes — build/v0.2-a16-v1-real-root only, no PR, no merge, no tag, no release, no history rewrite
NEXT: independent control only
