# Action suivante

## Exécuter TASK-0033 avec Claude Code

`TASK-0032` est désormais `VERIFIED` dans sa portée par `ACTION-0049`.

La branche de travail suivante est :

`build/v0.2-a17-v1-topographic-ux`

`TASK-0033 — V1 Progressive Topographic UX` est `READY` et encadrée par `DEC-0034` ainsi que par la référence UX assainie de l'ancien FileTopo.

**Action unique suivante :** demander à **Claude Code** d'exécuter intégralement `.orchestrator/NEXT_PROMPT.md` sur cette branche.

Claude Code doit implémenter, tester, documenter, pousser son travail sur la branche et terminer par son rapport dans `.orchestrator/RESULT.md`. Il ne doit jamais auto-déclarer la tâche `VERIFIED`.

Après son rapport : contrôle indépendant par l'orchestrateur avant toute tâche suivante.
