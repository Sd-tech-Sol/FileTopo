{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. The dialogue and clipboard-manager plugins are initialised for the Rust side only; their frontend commands are deliberately NOT granted - DEC-0033 H, TASK-0035 C.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
